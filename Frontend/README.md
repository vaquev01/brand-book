# Frontend

Estado atual seguro:

- A aplicação de interface continua operando em `src/` com Next.js.
- Esta pasta foi criada como destino arquitetural para uma futura separação explícita do frontend.
- O `tailwind.config.js` e `tailwind.config.ts` já incluem `./Frontend/**/*` para suportar essa migração sem quebra.
